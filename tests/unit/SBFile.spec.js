const fs = require('fs');
const path = require('path');
const expect = require('chai').expect;

const ui = path.join(__dirname, '../data/ui.toc');
const globals = path.join(__dirname, '../data/globals.toc');

describe('SBFile unit tests', () => {
    
});